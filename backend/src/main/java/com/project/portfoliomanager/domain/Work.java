package com.project.portfoliomanager.domain;

public class Work extends Event {


    public Work(int year, int length, String name, String type, String description) {
        super(year, length, name, type, description);
    }
}
